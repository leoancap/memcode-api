{
  "version": 2,
  "name": "memcode-api",
  "alias": "memcode-api.now.sh",
  "builds": [{ "src": "graphql/index.js", "use": "@now/node-server" }],
  "routes": [
    {
      "src": "/graphql",
      "dest": "graphql/index.js"
    }
  ],
  "build": {
    "env": {
      "redis": "@redis",
      "postgres": "@postgres"
    }
  },

  "env": {
    "redis": "@redis",
    "postgres": "@postgres"
  }
}
